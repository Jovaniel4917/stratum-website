# Suppress peer dependency warnings during install
# These warnings are harmless but can clutter build logs
# The warnings are from next-sanity's internal dependencies and don't affect functionality
legacy-peer-deps=false
strict-peer-deps=false

# Suppress ERESOLVE warnings (these are informational only)
fund=false
audit=false

